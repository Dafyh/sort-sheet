name: Coverage

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Test
      run: npm run test

    - uses: codecov/codecov-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: true